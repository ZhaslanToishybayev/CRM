targets:
  $default:
    sources:
      - lib/features/ai/**
    builders:
      freezed:
        generate_for:
          - lib/features/ai/models/**
      json_serializable:
        generate_for:
          - lib/features/ai/models/**
